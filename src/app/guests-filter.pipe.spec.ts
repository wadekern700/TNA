import { GuestsFilterPipe } from './guests-filter.pipe';

describe('GuestsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GuestsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
